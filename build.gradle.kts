// LSPlant项目构建配置 - PLT Hook库项目配置
val androidTargetSdkVersion by extra(34)           // 目标SDK版本
val androidMinSdkVersion by extra(21)              // 最小SDK版本
val androidBuildToolsVersion by extra("34.0.0")    // 构建工具版本
val androidCompileSdkVersion by extra(34)          // 编译SDK版本
val androidNdkVersion by extra("26.2.11394342")    // NDK版本
val androidCmakeVersion by extra("3.22.1+")        // CMake版本
